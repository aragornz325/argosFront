/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com', 'res.cloudinary.com'], // Agrega aquí los dominios permitidos
      },
};

export default nextConfig;
